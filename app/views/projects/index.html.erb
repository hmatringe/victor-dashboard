<div class="pull-right">
   <button type="button" class="btn btn-default btn-sm">Create project</button>
</div>
<h3>Projects</h3>
<div class="row">
 		<% @projects.each do |p| %>
 		<%= render partial: "project_card", locals: { project: p} %>
 		<% end %>
</div>
